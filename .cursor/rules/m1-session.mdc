---
description: Mu S2 Milestone 1 — resume context for VPS / new chats
alwaysApply: true
---

# Milestone 1 continuation

When the user continues Mu Online Season 2 / M1 work, read and follow:

- `m1/SESSION-HANDOFF.md` (authoritative status + next steps)
- `m1/VPS-MIGRATE.md` (what is/isn't in git)

## Hard constraints

- M1 scope only: stack + client connect + Lorencia + combat + relog + one recording. No web admin / events UI.
- Do **not** repeat dead loops: editing packed `main.exe` IP strings and blind relaunch.
- `play-safe\main.exe` is ASPack-packed; use Release `Main.dll` inject (`m1/scripts/LaunchMu.cs`) after unpack.
- ConnectServer UDP F4 server-list support is already patched in `SocketManagerUdp.cpp/.h`.
- Current blocker: client still never sends to ConnectServer despite live IP `127.0.0.1` after successful EntryProc.

## Credentials

- Account `test` / `test123`, serial `k5lEopalwaudns8h`, version `1.02.03`
